import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'assets.aceternity.com', 'www.shutterstock.com', 'plus.unsplash.com'],
  },
};

export default nextConfig;
